{
  "name": "@plannotator/chrome-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp manifest.json popup.html dist/ && bun build src/popup.ts --target browser --outfile dist/popup.js",
    "test": "bun test src"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "typescript": "~5.8.2"
  }
}
