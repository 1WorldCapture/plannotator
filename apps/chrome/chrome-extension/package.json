{
  "name": "@plannotator/chrome-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/assets && cp -R assets/icons dist/assets/icons && cp manifest.json popup.html dist/ && bun build src/popup.ts --target browser --outfile dist/popup.js",
    "package": "bun run build && rm -f plannotator-clipboard-extension.zip && cd dist && zip -X -r ../plannotator-clipboard-extension.zip .",
    "test": "bun test src"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "typescript": "~5.8.2"
  }
}
